# V0.2 统一数据服务（market-data-service）实现计划

> **面向 AI 代理的工作者：** 按 `docs/DEVELOPMENT_PROCESS.md` 逐任务实现；核心逻辑 TDD。
> 依赖：V0.1a 数据管线 + V0.1b 视图 + V0.3 策略引擎（facts/strategy/pool/events 已就绪）。

**目标：** 服务器本地进程（127.0.0.1:8787，nginx 反代 `/DSH/api/`）统一提供数据 API：读 `data/web/` 视图层 + `data/facts/<date>/` + `data/kline/`，带 meta（data_date/source/fetched_at）与内存缓存；开发期本地 8787 验证（形态 B），部署期 NSSM 注册（形态 A）。

**架构：** 纯 stdlib `http.server`（零第三方依赖，服务器免 pip install）——路由 → 读文件（lru 缓存）→ JSON 响应（支持 gzip）；前端静态优先、API 补动态（历史个股时间线 / 实时快照 / Agent 入口）。

**技术栈：** Python 3.10 stdlib、`http.server`、`functools.lru_cache`、pytest（in-process 起服务测试）。

---

### 任务 1：服务骨架与只读路由（TDD）

**文件：**
- 创建：`services/market_data_service.py`
- 测试：`tests/test_market_data_service.py`

- [x] **步骤 1：写失败测试（in-process 起服务 → 断言响应）**
```python
def test_health(client):
    assert client.get("/api/health")["status"] == "ok"
def test_day_latest(client, tmp_data):
    v = client.get("/api/day")            # 无 date → 最新（index.json 第一个日期）
    assert v["data"]["date"] and "sectors" in v["data"]
def test_day_meta(client):
    r = client.get("/api/day?date=2026-08-14")
    assert r["meta"]["data_date"] == "2026-08-14" and "source" in r["meta"]
```
- [x] **步骤 2：确认失败 → 3：实现**：`HTTPServer` + 路由（`/api/health`、`/api/days`、`/api/day?date=`、`/api/instruments`、`/api/strategies`、`/api/pools`、`/api/events`、`/api/limitups`、`/api/sectors`、`/api/market`、`/api/index`、`/api/ladder`、`/api/abnormal`、`/api/money-flow`、`/api/leading-reason`、`/api/kline/<sid>`）+ lru 缓存 + gzip（Accept-Encoding）
- [x] **步骤 4：确认通过**：`pytest tests/test_market_data_service.py -v` → PASS
- [ ] **步骤 5：Commit** `feat: market_data_service 骨架与只读路由（lru + gzip）`

### 任务 2：历史个股时间线与实时快照

- [x] **步骤 1：写失败测试**：`/api/history?stock=SZ300487` → 聚合该股跨日期（strategy/pool/limitup/events）时间线；`/api/intraday/latest` → 解析 `snapshots.ndjson` 末行
- [x] **步骤 2：确认失败 → 3：实现**：`history_for_stock(sid)` 遍历 `facts/*/`（strategy.json/pool.json/limitup.json/events.json）按日期聚合；`intraday_latest()` 读最新 intraday 目录 ndjson 末行
- [x] **步骤 4：确认通过**；**步骤 5：Commit** `feat: /api/history 个股时间线 + /api/intraday/latest`

### 任务 3：真实运行 + 部署文档

- [x] **步骤 1：本地起服务**：`python services/market_data_service.py --port 8787`（后台任务）→ curl 验证 health/day/strategies/pools/history
- [x] **步骤 2：nginx 配置**：`/DSH/api/` 反代已写 DEPLOY.md；补 NSSM/计划任务注册说明（形态 A 部署）
- [ ] **步骤 3：Commit + tag** `feat: V0.2 统一数据服务` → `v0.2.1`
