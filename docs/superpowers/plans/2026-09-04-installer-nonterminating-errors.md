# 1.0.41 安装器非终止错误与清理权限

截图与本地1.0.40一致：Register-ScheduledTask报错后仍执行scheduled_task分支，Remove-Item再次拒绝访问。修复系统命令显式ErrorAction Stop及实际注册返回值检查；清理旧启动项失败保留文件，标记永久守护未验证，不阻断健康主程序。任务启动失败改为会话守护。

先测试错误返回、旧文件清理拒绝、启动任务拒绝，再改代码。保留已有互斥/重试预算/会员授权与数据；无提权、ACL或策略变更。真实受限身份重跑用户H:\JinshiDSH下主安装器；发布1.0.41完整包并核对哈希。提交仅本任务改动。
