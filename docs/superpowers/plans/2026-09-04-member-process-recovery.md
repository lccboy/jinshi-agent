# 会员后台停止后的有界恢复

1. 恢复已安装的 1.0.38，不修改会员数据和授权。
2. 先测试再实现独立 PowerShell 单次守护：默认不启动，显式 Enabled 后检查8790和精确安装路径进程；健康或进程仍存在时不重启。
3. 命名互斥防止重复守护；15分钟最多启动3次，失败局部熔断。每分钟执行一次，健康时不写日志、不扫描数据、不请求上游。
4. 仅在会员根目录 runtime 写守护状态；诊断日志限制1MB加一个备份。不记录会员凭据或行情。无新依赖，无API变更，数据模型与策略口径不变。
5. 通过当前用户计划任务安装并启用，登录触发与一分钟周期，隐藏运行。读取install_state选择当前版本，不固定旧版本；启动失败/停止记录时间，不臆测退出原因。
6. 验证关闭态、重复调用、故障熔断与恢复，真实8790健康与页面；不发布8080或公共服务器。运行回归测试并限定提交推送。
7. 回退：禁用 JinshiDSH-MemberRecovery 当前用户任务；不删除业务数据、不结束健康工作台。
