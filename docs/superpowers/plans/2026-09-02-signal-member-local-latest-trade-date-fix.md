# 实时信号会员本地最新交易日修复计划

1. 对比实时接口、会员本地实时结果与页面实际 DOM，确认日期门禁导致的静默回退。
2. 先补失败测试，要求实时模式接受最新有效交易日，并明确区分当天实时与最近收盘。
3. 最小修改信号视图合成逻辑，禁止沿用此前选中的历史日期，并保留会员本地预警。
4. 升级会员工作台，发布服务端并安装到 8790。
5. 验证 30 条会员可买预警、显示日期/状态、接口与浏览器 DOM，跑全量测试后提交推送。
