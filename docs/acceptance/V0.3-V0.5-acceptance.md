# V0.3 / V0.5 生产验收记录（2026-08-17）

## V0.3 交易工作台

- 主数据：active 5422、source_missing 108、invalid_instrument 192；板块覆盖 100%，行业覆盖 91.6083%，上市日期覆盖 98.2294%。
- 题材库：动态发现 `金十AI题材库/all_themes_slim.json`，248/248 更新成功，manifest 记录路径、mtime、hash 和 freshness。
- 策略：17 模型全市场扫描；alert 30、candidate Web 裁剪 100；⑧金量公式未修改；完美十法门槛恢复为 11。
- 日视图：2026-08-14 raw 87.4KB、gzip 8.9KB，质量门禁全部 PASS。
- 浏览器：实时信号、题材库、板块强度、策略模型、历史选股五页均真实渲染；股票名称正确；策略 TOP20；console error/warn 为 0。
- 调度：任务计划注册受本机安全策略拒绝，已启用 HKCU 登录自启用户态守护；心跳、阶段幂等、失败重试、盘中时窗、日志轮转可用。
- 主源保护：盘前使用上一工作日 KPL 截面；空响应或少于既有 active 的 80% 时写盘前失败。验收期间真实捕获一次当前日空响应并恢复到 5422 active。
- 自动化测试：159 项通过（最终提交前回归）。

连续 5 个真实交易日无 fail 属于时间型 soak 指标，代码能力已交付，但本记录不伪造未来运行结果；由用户态调度持续累积 `data/runs/quality_<date>.json`。

## V0.5 生产化

- 网络边界：API 只绑定 127.0.0.1；nginx 只允许本机及 RFC1918 内网网段。
- 密钥：仓库、前端和 dist 扫描 0 命中；运行配置不含凭据。
- 备份：真实备份 42 个不可再生/发布文件，SHA-256 全部通过；隔离恢复 42/42 通过。2.3GB K 线为可重建缓存，按 vipdoc 重同步恢复。
- 回滚：本机部署自动生成 `C:\nginx\backups\DSH.<timestamp>`；已完成一次真实回滚并 HTTP 200 验收，再部署当前版本。
- 守护：用户态 scheduler 监控 API/nginx，异常时自动拉起；日志写入 `data/runs/logs` 并按大小轮转。
- 部署：Web 与 `/DSH/api/health` 均返回 HTTP 200，nginx reload 使用明确 prefix/config 并检查退出码。

## 运维入口

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\status.ps1
python -m services.collector.production_ops scan-secrets --root .
python -m services.collector.production_ops backup --root . --out backups --keep 14
```
