# 1.0.39 安装守护集成发布验收

## 已验证
- 全量测试：639 passed，73.96秒。
- 真实PowerShell隔离目录：首次安装、保留自定义data_root、升级、同版重装保留previous_version、回滚、卸载保留授权文件。
- 模拟操作系统权限结果：任务拒绝后生成Unicode用户启动项；两种持久化均失败返回非零，不输出安装成功。
- 真实PowerShell守护：连续4次缺进程检查仅允许3次启动；disabled状态不更新运行状态。
- 端口占用时后台启动在任何同步/归档线程启动前失败，Windows采用SO_EXCLUSIVEADDRUSE。
- 构建ZIP：266个清单条目全部哈希一致，两个守护脚本均在根目录，主安装器自动调用。
- 冻结程序8792冒烟：健康通过、8个TAB、JS资源200、eltdx/_native.pyd存在；测试进程已结束。
- 8790：1.0.39健康，首页缓存版本0.4.83-member-recovery；主安装器授权文件哈希检查通过，保留1.0.38回滚路径。
- 8080：只更新前端版本、指南与会员下载文件；公共采集及数据未改动。
- 公共服务器：定向发布8个前端/下载文件并逐个SFTP核对；再次从公网读取ZIP计算SHA256，与构建一致。
- ZIP：10,850,528字节；SHA256 B7781FD43790CD7E21EEED0AD3A456B4F26153425BD420AC7891B471EFF4C5EE。

## 明确未完成的本机条件
- 当前执行身份被Windows拒绝注册计划任务及写入启动文件。新版主安装器按设计返回安装未完成，而非成功。
- 服务已升级并健康运行，另启动当前会话守护；这不等同于永久守护配置成功。
- 用户需以管理员身份运行ZIP内的install-member-workbench.ps1完成本机永久自启动。已覆盖同版重装，不丢失回滚版本。
- 未实际注销/重启用户电脑；未对交易中的生产进程做强制崩溃测试。自动恢复预算采用隔离注入验证。
- bsk浏览器连接数为0，未进行DOM验收；仅验证HTTP首页、资源与真实冻结程序服务。
- 此次未修改行情、策略或归档产出，未运行会生成归档的collector --verify。

## 审查结论
安装与守护共享互斥锁；只结束本安装目录进程；守护任务/启动入口统一管理；失败不误报成功。
本次只发布公共下载程序，不上传会员数据。原始进程退出原因仍缺乏历史日志证据，不能据此认定此前退出原因。
