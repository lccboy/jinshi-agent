# 1.0.41 安装错误处理验收

- 用户截图脚本路径H:\JinshiDSH，元数据为1.0.40，与发布源一致。错误为Register-ScheduledTask拒绝后仍走scheduled_task分支，以及旧启动项Remove-Item拒绝。
- 系统任务命令改为显式ErrorAction Stop，检查注册返回结果；旧入口删除失败不破坏文件，返回cleanup_pending，不宣称永久自启动完成。任务启动失败启动会话守护。
- 新增失败测试后修复：注册空返回、旧启动项删除拒绝；所有PowerShell测试互斥锁使用独立测试名称，避免与真实安装竞争。
- 全量642 passed，74.94秒。此前一次测试与实际安装共用互斥造成失败，已修复测试隔离后重跑。
- 用户目录脚本更新前备份为*.bak.20260904-144225。先在实际目录确认1.0.40不再红字；再更新完整1.0.41载荷与元数据，在相同目录安装通过。
- 升级中一个旧1.0.40进程路径不可读但持有8790；通过端口、进程名和健康API确认身份后单独停止，再运行主安装器，健康版本1.0.41。
- 当前永久自启动权限仍受限，输出PARTIAL/session_only；这不是永久恢复已验证。会员授权校验与数据路径保留。
- ZIP SHA256：49C75BD81FD3EA49CD010C2C3F4B9BA5F282653FC2FC65A771254692B1F08BB0。
- 8080只更新前端下载文件。服务器定向发布8文件，不更改公共采集或数据。
