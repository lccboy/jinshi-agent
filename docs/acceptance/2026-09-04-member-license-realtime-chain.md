# 1.0.42 会员授权与当日数据链验收

## 原因与修复
- 授权有效期未到，但checked_at停在9月3日12:12，超过24小时离线门禁；服务没有后台续验，阻断会员读取与计算。
- 升级重启后旧页面cookie失效，POST返回local token required；页面此前只显示HTTP403。
- 本机启动误依赖浏览器保存的激活码；改为读取持久授权，必要时同设备云校验。
- 收盘后策略视图无条件退出当天；新增仅限本机当天有效数据的快照展示。
- 授权维护独立线程，6小时校验、失败5分钟退避、15秒网络超时；不增加行情请求/策略计算频率，不放宽授权规则。

## 验证（2026-09-04）
- TDD先验证缺失续验和POST单次恢复，再实现；全量647 passed，76.85秒。
- 真实现有授权云端validate成功；无cookie请求403，新首页会话后成功，未打印或发布凭证。
- 8790现有服务恢复后：2026-09-04 14:59:40，actionable_alerts 30；history_pool_summary重点30/30，候选361/100；strategy_all日期2026-09-04，list 391。
- 本机signal兼容接口179ms、154336字节（单次观测，非性能分位数）。内置浏览器真实可买预警页面已显示中文名称、模型和星级。
- 1.0.42完整冻结包在隔离8792端口启动成功，8个TAB、资源200、eltdx原生扩展存在。
- 服务器仅定向更新8个前端/下载文件，SFTP逐文件哈希一致；公开latest清单1.0.42。
- ZIP SHA256：C06E08B5BCB45E55B29341DFEE64B9A6A1E00E1DB825D19FC5CFAADA40864515。

## 未闭环项
- 本机旧1.0.41进程PID8632仍占8790。当前工具会话是Medium Integrity，Stop-Process被Windows拒绝；未绕过权限、未强杀其他进程。
- 新安装载荷已更新到H:\JinshiDSH，已请求用户在管理员PowerShell运行主安装器。1.0.42后台持续续验与四页升级后浏览器验收仍待安装接管。
- 永久守护仍session_only，此次未声称已修复计划任务/启动目录权限问题。
- 不对会员历史归档执行重算/覆盖；本次未运行会产出归档的archive_job --verify，改由测试覆盖授权与实时投影边界。
